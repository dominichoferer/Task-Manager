{
  "name": "taskflow-desktop",
  "version": "0.1.0",
  "description": "TaskFlow – Dein smarter Task Manager",
  "author": "TaskFlow",
  "main": "src/main.js",
  "scripts": {
    "dev": "TASKFLOW_URL=http://localhost:3000 electron .",
    "dev:win": "set TASKFLOW_URL=http://localhost:3000 && electron .",
    "start": "electron .",
    "build": "electron-builder --mac --x64 --arm64",
    "build:win": "electron-builder --win",
    "build:linux": "electron-builder --linux"
  },
  "build": {
    "appId": "com.taskflow.app",
    "productName": "TaskFlow",
    "electronVersion": "33.3.1",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "assets/**/*"
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "assets/icon.png",
      "target": [
        {
          "target": "dmg",
          "arch": ["x64", "arm64"]
        }
      ]
    },
    "win": {
      "icon": "assets/icon.png",
      "target": "nsis"
    },
    "linux": {
      "icon": "assets/icon.png",
      "target": "AppImage"
    },
    "dmg": {
      "title": "TaskFlow ${version}",
      "window": {
        "width": 540,
        "height": 380
      }
    }
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  }
}
